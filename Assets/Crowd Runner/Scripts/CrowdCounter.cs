using UnityEngine;
using TMPro;

public class CrowdCounter : MonoBehaviour
{
    [Header("References")]
    [SerializeField] private TextMeshPro crowdCountText;
    [SerializeField] private Transform crowdCountField;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
    }
}
